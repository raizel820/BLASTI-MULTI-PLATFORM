---
Task ID: 1-7
Agent: main
Task: Fix web app "data failed to load" error, consolidate initial sync, fix first-login state machine, add snapshot sequence semantics

Work Log:
- Audited entire monorepo structure (apps/web, apps/api, apps/desktop, packages)
- Discovered root cause of "data failed to load": Next.js API rewrites were removed, causing 404 for API requests when not going through Caddy gateway
- Added API rewrites back in next.config.ts to proxy /api/* and /socket.io/* to port 3003
- Found and fixed buildUrl() double-? bug: paths with existing query params were getting ?XTransformPort appended instead of &XTransformPort
- Found and fixed Bearer token auth issue: Next.js rewrites don't forward Set-Cookie headers, so httpOnly cookie auth doesn't work through the proxy
- Added module-level token cache in api-client.ts (setSessionToken/clearSessionToken) to avoid timing issue with Zustand persist
- Updated login-form.tsx to call setApiClientToken() immediately after successful login
- Updated use-app-store.ts logout to clear cached token
- Audited duplicate initial sync mechanisms: found initial-sync.js (proper) and sync-service.js's initialSync() (conflicting)
- Removed duplicate initialSync() from sync-service.js
- Fixed setAuth() in sync-service.js to not reset sync version or call _syncCycle() after login
- Added concurrency guard to initial-sync.js
- Removed syncService.initialSync() call from index.js
- Fixed first-login decision logic in loading-screen.js (NOT_INITIALIZED is not an error)
- Added sync:initial-required event in sync-service.js when agency not initialized after login
- Added snapshot sequence recording in initial-sync.js (stores initialSyncSnapshotSequence in _sync_meta)
- Added snapshot sequence as starting cursor in sync-service.js (_pullFromCloud checks for snapshotSequence)
- Added race condition check in initial-sync.js (re-checks snapshotSequence after all stages complete)

Stage Summary:
- "Data failed to load" error fixed: API rewrites + Bearer token auth working
- Initial sync consolidated to exactly ONE implementation (initial-sync.js only)
- First-login state machine: NOT_INITIALIZED → trigger initial sync → READY → incremental sync starts
- Snapshot sequence semantics: initial sync records snapshotSequence, incremental sync uses it as starting cursor
- Race condition handling: re-checks snapshotSequence after completion, emits warning if changed
- All agency dashboard API calls returning 200 with proper auth

---
Task ID: 1
Agent: main (Z.ai Code)
Task: Import https://github.com/raizel820/BLASTI-MULTI-PLATFORM.git as the main project, read all .md docs, and run the app in the sandbox

Work Log:
- Cloned BLASTI-MULTI-PLATFORM (master) to /tmp, explored monorepo (apps: web/api/desktop/mobile, packages: db/cloud-db/core)
- Read all 13 root .md docs + apps/*/README.md + agent-ctx logs (via Explore subagent): BLASTI is an Arabic-first smart queue-management SaaS for Algerian institutions (hash-based SPA in apps/web page.tsx, Hono+Socket.IO API on 3003, Prisma SQLite dev DB with @blasti/cloud-db PostgreSQL fallback to SQLite when CLOUD_DATABASE_URL unset)
- Replaced the previous Next.js scaffold in /home/z/my-project with the monorepo (rsync minus .git/out/node_modules), preserved sandbox infra (.env, Caddyfile, .zscripts, upload/download, mini-services, skills)
- Wrote root .env + apps/api/.env + apps/web/.env (absolute DATABASE_URL=file:/home/z/my-project/packages/db/data/custom.db, NEXTAUTH_SECRET, INTERNAL_SECRET, API_PROXY_URL=http://localhost:3003; CLOUD_DATABASE_URL intentionally unset for SQLite fallback)
- Hardened root package.json dev:api script to export DATABASE_URL/NEXTAUTH_SECRET/INTERNAL_SECRET/CORS_ORIGIN for the whole chain (bun only auto-loads .env from each app's cwd)
- bun install (2333 pkgs), prisma generate + db:push (42 tables), DB came pre-seeded (admin/admin123, owner1/owner123, staff1/staff123, customer1/customer123 + 4 agencies DEMO001/CLINIC001/LAB001/GOV001) — db:seed failure was duplicate-data only
- Discovered sandbox constraint: processes spawned by tool commands are reaped ~3s after the command exits; dev.sh (run by platform at session start) is the sanctioned way dev servers persist. All verification was therefore done inside single long-running commands hosting the server
- Fixed schema drift bug: API code (queue.ts:399 + agency/admin/sync routes) selects Reservation.createdAt but both Prisma schemas only had joinedAt — added `createdAt DateTime @default(now())` to Reservation in packages/db AND packages/cloud-db schemas, db:push (additive, no data loss)
- Extended apps/web/next.config.ts rewrites: added exact '/socket.io' + '/health' proxies to API_PROXY_URL (3003)
- Verified via agent-browser + curl: home page renders (Arabic RTL), login works for admin/owner1/staff1/customer1, admin dashboard (#/admin) and agency dashboard (#/agency) render with real data, customer dashboard (#/customer) shows real activity, onboarding tour modal works
- Golden path E2E via API: customer1 join queue → ticket C-007 WAITING with ETA; owner1 call-next → CALLED with calledAt; queue/status healthy; admin stats return 4 agencies/4 users; then restored C-006/C-007 to WAITING for a clean demo queue
- Realtime verified: socket.io polling handshake OK on :3003 direct and via Caddy gateway :81 with XTransformPort=3003; FULL WebSocket upgrade through the gateway proven with socket.io-client E2E test (connects, sid assigned). Note: Next.js dev rewrite does NOT proxy /socket.io on direct localhost:3000 access (Next 16 limitation) — irrelevant for users because the preview routes through the Caddy gateway where WS works; UI has polling + offline fallback anyway

Stage Summary:
- BLASTI monorepo is now the main project at /home/z/my-project and runs with plain `bun run dev` (concurrently: web :3000 + api :3003)
- Logins: admin/admin123 (super admin), owner1/owner123, staff1/staff123, customer1/customer123 — agencies DEMO001, CLINIC001, LAB001, GOV001
- Fixed: Reservation.createdAt schema drift (both schemas), dev:api env export, per-app .env files, /health + /socket.io rewrites
- 0 prisma errors / 0 5xx across all endpoints after fixes; DB at packages/db/data/custom.db (pre-seeded)
- Sandbox persistence: the platform's .zscripts/dev.sh auto-runs `bun run dev` at session start — BLASTI will come up automatically on the next session; env is fully configured for that path (root db:push uses inline absolute DATABASE_URL, unaffected by start.sh overwriting root .env)

---
Task ID: 2
Agent: main (Z.ai Code)
Task: Fix BLASTI Web dev crash — `bun run dev:web` → ChunkLoadError → browser crash; find root cause, fix dev scripts, bound client loops, add regression protection

Work Log:
- Root cause identified: root dev:web used `bun --bun run node_modules/.bin/next dev` — Next.js 16 dev server ran on Bun's JS runtime (unsupported combo). Evidence: start-server process under Bun with 1.4GB RSS runaway + .next/_events NEXT_CLI_SESSION_STOPPED after 459ms
- Fixed root package.json dev:web → `node node_modules/next/dist/bin/next dev -p 3000 -H 127.0.0.1`; added dev:web:api, dev:web:lan, clean:web, test:web:startup
- Fixed apps/web package.json dev → Node runtime + 127.0.0.1, added dev:lan + clean
- next.config.ts: rewrites return [] in NEXT_BUILD_MODE=export (fixes desktop/mobile static export builds); /socket.io rewrite destination re-appends trailing slash (Next 308-normalizes /socket.io/ → /socket.io BEFORE rewrites; engine.io only answers /socket.io/ → realtime could never connect on direct :3000 access)
- use-realtime.tsx: transports websocket-first → polling-first (WS-first hangs against Next dev forever — upgrade TCP accepted, never completed — so polling was never reached); reconnectionAttempts Infinity → 20 on web; one bounded reconnect per sessionToken change
- Deleted src/hooks/use-realtime.ts (dead duplicate shadowed by .tsx, with divergent reconnect configs — tsc checked .ts while bundler ran .tsx)
- use-discovery-service.ts: 3s scan-status poll now stops after 3 consecutive failures (was unbounded if API died mid-scan)
- connection-status.tsx: health checks honor central isApiUnreachable() 30s cooldown
- New src/components/shared/dev-debug-hud.tsx — opt-in diagnostics (?debug=1 / localStorage blasti:debug=1, Alt+Shift+D hides): API reachability, socket status, request counter, route, JS heap; mounted in all 4 page.tsx render branches, inert unless enabled and in production
- New scripts/web-startup-test.mjs + bun run test:web:startup: spawns dev:web, verifies ready/200/all chunks 200+non-empty/chunk byte stability/bounded second load/idle RSS ceiling — 8/8 passed from a clean .next with API NOT running
- start.sh aligned to Node runtime + 127.0.0.1; daemon-start.sh already safe (delegates to fixed apps/web dev script)
- Audited via 2 subagents: all 48 setInterval sites cleared-or-dormant; socket singleton ref-counted with cleanup; NO server-only packages (Prisma/cloud-db/Node builtins) in client bundle; all 31 lazy() targets exist with resolvable deps; api-client retries bounded (3, exp backoff, 4xx no-retry, 30s unreachable cooldown persisted in sessionStorage)
- Verified via agent-browser on http://127.0.0.1:3000 direct: landing renders, owner1 login → agency dashboard live, realtime CONNECTED over polling ("متصل بالخادم"), 0 page errors, 46/46 resources OK, browser heap 58.7MB, server RSS flat (Δ+1MB/40s idle); web-only mode (API down): graceful error states, bounded single retry chains, page stays interactive; request volume finite (91 req first 10s incl. dev assets; steady state stats 3/health 2/socket ~4/notifications 2 per 35s)
- Final stack restarted fresh: web (Node, 127.0.0.1:3000) + API (127.0.0.1:3003); gateway :81 → web 200 confirmed with loopback binding

Stage Summary:
- Root cause: Bun-runtime Next.js dev server (bun --bun) — chunk-serving corruption + dev-server memory runaway; amplified on Windows by 0.0.0.0 binding, WS-first socket hang, unbounded reconnect, uncoordinated pollers
- bun run dev:web is now standalone-safe: Node runtime, exactly ONE server, 127.0.0.1, no API/Electron dependency, graceful when API is down
- Realtime now works on direct dev access (was silently broken: slash-less /socket.io 404 + WS-first hang)
- Regression protection: bun run test:web:startup (8/8) + opt-in dev HUD (?debug=1)
- Full report: /home/z/my-project/WEB_DEV_CRASH_AUDIT.md

---
Task ID: 2-b
Agent: Explore-api-parity-auditor
Task: Cloud API vs Local API agency parity deep audit
Work Log:
- Extracted full route inventory: cloud apps/api/src/index.ts:362-392 mounts 30 route modules; extracted every app.get/post/put/patch/delete from 18 agency-relevant cloud route files with line numbers
- Extracted every local route registration from apps/desktop/local-api/index.js (8305 lines) incl. 3 for-loop alias registrations (2073, 2315, 3388) that plain greps miss
- Confirmed apps/desktop/local-api-routes.js is DEAD CODE: header @deprecated (lines 1-19), not required by main.js (requires only ./local-api/index, ./local-api/sync-service, ./local-api/initial-sync, ./local-api/lib/db at lines 1064-1376) nor by local-api/index.js; still shipped by electron-builder.yml:18
- Verified all 7 claimed fixes (a)-(g) in code with line numbers; found fix (c) partially broken (7 call sites use stale logPendingMutation signature) and fix (d) functionally ineffective (random per-call idempotency key can never dedupe)
- Compared business logic line-by-line: call-next (cloud queue.ts:49-210 + agency.ts:1357-1532,3668-3730 vs local 2731-2909), walk-in (cloud agency.ts:1608-1809 vs local 4220-4405), reservation create (cloud reservations.ts:25-240 vs local 2452-2614), postpone (cloud reservations.ts:773-871 vs local 6409-6513), complete/no-show/cancel (cloud agency.ts:1868-1994 vs local 2960-3160), staff CRUD (cloud 2522-2932 vs local 4907-5016,5744-5856), branches (cloud 411-564 vs local 2025-2151), counters (cloud 569-765 vs local 2158-2376,4720-4802), services (cloud 2191-2352 vs local 3995-4039,5688-5738), profile/settings/working-hours (cloud 1200-1255,2423-2482,3625-3664 vs local 1560,1758,4803,3647,4865,5035-5086), stats (cloud 2936-3131 vs local 3843-3992), analytics/daily-chart/history (cloud 188,769,3138 vs local 4468,4839,4505)
- Grepped local for stubs/TODO/hardcoded values; audited subscription read path (local 4621-4713) and QR code (5859-5893)
- Verified Prisma schema ground truth (packages/db/prisma/schema.prisma): Counter has NO agencyId (724-743), Reservation has NO waitTime field (396-463), Counter.branchId required (731)
- Cross-checked desktop frontend contract (apps/desktop/frontend/src/api/client.ts:130-507) to rank which local gaps actually break the desktop UI
Stage Summary:
- Route parity ~90%: local missing cloud subscription pay/cancel/unsubscribe, hardware (x3), enterprise-request (x2), GET /api/reservations/active, /api/devices register, all ~50 /api/agency-devices/* LAN routes; local PUT /api/reservations/:id/status is PATCH-only (method mismatch); AGENCY_API_PARITY_REPORT's claimed local /api/agency/devices|tv|printers routes DO NOT exist
- 3 latent Prisma-breaking bugs remain despite audit claiming fixes: local index.js:2534 counter.count({agencyId}) (reservations POST 500s), 1647 dashboard counter.findMany({agencyId}) (silently swallowed → activeCounters always []), 2364 counter delete clears non-existent activeReservationId instead of currentReservationId (no-op)
- 7 logPendingMutation call sites use legacy ('update','Model',id,data) signature (6249,6270,6290,6308,6380,6402,6507) → replay fetch(method='update') always fails → services PATCH via /api/services/:id and 6 legacy reservation actions NEVER sync to cloud
- Subscription enforcement bypassable: legacy action routes /api/reservations/:id/complete|noshow|cancel|call|recall (6257-6407) lack requireActiveSubscription() and are the exact paths desktop client.ts:281-293 calls
- Offline token import path lost HMAC: local import-walk-in (7018) and walk-in-token (5639) never verify HMAC signature/expiry (cloud reservations.ts:1350-1354 uses verifyQRToken); walk-in-token semantics diverge entirely (local calls ticket, cloud mints token)
- Queue numbering divergence: local POST /api/reservations uses global lastIssuedNumber (2520-2522) while cloud + local walk-in number per-service (4334-4338) → cross-service collisions on desktop-created tickets
- Postpone: local enforces max-3 (6431-6434) but cloud has NO limit (reservations.ts:773-871 — parity report claim false); local 3-step shift not transactional (6455-6472) vs cloud $transaction (816-861)
- Local does MORE than cloud in 2 spots: branch delete deactivates counters (2134-2139; cloud doesn't), service delete cancels WAITING reservations silently w/o notifications (5721-5729; cloud doesn't)
- Stats parity report overstated: rating source (Review vs Reservation.rating), rate windows (today vs all-time), wait metric (joinedAt→calledAt vs →completedAt), activeCounters staffId filter, NO_SHOW date field (skippedAt vs cancelledAt) all differ; local /api/agency/analytics 500s on non-existent waitTime field (4482-4485)
- Fixes (a),(b),(e),(f),(g) CONFIRMED-IN-CODE (requireActiveSubscription 506 + 19 sites; aliases 3499/4803/4106/4154/3370; OFFLINE_TOKEN_MAX_MS 99/452-479; markCloudContact 8271 + sync-service.js:1551; SYNC_TABLES sync-service.js:17-27 & SYNC_MODELS sync.ts:47-57); (c) PARTIAL; (d) INEFFECTIVE

---
Task ID: 2-c
Agent: Explore-sync-auditor
Task: Sync engine (cloud DB vs local DB) deep audit
Work Log:
- Read worklog 1-7 context; read fully: local-api/sync-service.js (1849 ln), initial-sync.js (1040), index.js sync parts (WAL 576-693, sync routes 5390-5509, markCloudContact 8271), main.js, loading-screen.js (diagnostics flow), cloud-side sync.ts (861), initial-sync.ts (539), sync-helpers.ts (626), offline-sync lib+routes, eccp-reconciler.ts, packages/core sync-registry/sync-serializer, desktop frontend sync store + 4 components, web db/sync.ts + sync-service.ts + offline-layer + use-offline-sync + indicators
- Traced wire protocol end-to-end and found contract mismatches: desktop pull sends `sinceSequence` but cloud pullSchema (sync.ts:108) has only `cursor/lastPulledAt/agencyId/limit` → sinceSequence silently stripped; desktop push reads `pushResponse.accepted/conflicts` (sync-service.js:1290-1293) but cloud returns `{results}` (sync.ts:743) → _lastPushedVersion never advances
- Verified SyncChange is ONLY written by processPushMutation (grep: zero call sites of recordSyncChange/atomicMutation in business routes) → cloud-originated changes (web/kiosk/QR/cron) never appear in pull
- Verified Task 1-7 snapshot-sequence work present (initial-sync.js:928, sync-service.js:800-821) but defeated server-side by cursor stripping + shared server-side SyncCursor
- Confirmed legacy dual-DB: cloud-sync.js marked @deprecated, required by nobody; local-db.js only required by cloud-sync.js; local-api-routes.js unreferenced (all still packaged in electron-builder.yml:18-20)
- Confirmed packages/core sync-registry/sync-serializer ARE imported by cloud (sync.ts:24, initial-sync.ts:31-32, sync-helpers.ts:20) but NOT by desktop sync-service.js (divergent duplicate lists)
- Confirmed desktop ConflictResolutionDialog + sync-indicator.tsx and web OfflineSyncIndicator + lib/sync-service.ts + offline-queue.ts are unmounted/unreferenced (dead UX paths)
- Mapped model coverage: desktop SYNC_TABLES == cloud SYNC_MODELS (19, match); initial-data stages only 13 → 6 synced models never backfilled; AuditLog written locally at 12+ sites but excluded from sync; local schema (packages/db) has syncVersion/syncedAt/AgencyLocalState/DeletedRecord
- Found poison-pill WAL entries: logPendingMutation('update','Reservation',id,...) legacy convention at index.js:6249/6270/6290/6308/6380/6402/6507/6646 used by the exact endpoints the frontend calls (client.ts:277-297) → replay builds invalid URL → 5 failures → 'abandoned' silently
- Checked auth/offline policy: 3-day OFFLINE_TOKEN_MAX_MS (index.js:99), 401 in incremental sync only hits generic backoff (no re-auth event); initial-sync handles AUTH_FAILURE properly
Stage Summary:
- P0: cloud pull advances a SHARED per-agency SyncCursor on send (sync.ts:252-253) and ignores the desktop's sinceSequence (zod strips it) → crash/multi-client data loss; Task 1-7 baseline ineffective end-to-end
- P0: no cloud business route records SyncChange → pull only ever returns echo of desktop's own pushes → desktop never receives cloud-side changes
- P0: push response contract mismatch → _lastPushedVersion never advances → entire agency dataset re-pushed every 2-min cycle, SyncChange/syncVersion churn unbounded
- P1: poison-pill WAL mutations from queue-op endpoints ('update'/'Model' convention) fail 5× then abandoned silently = data loss; partial mitigation only via diff push
- P1: LOW_FREQUENCY model filter skips changes but cursor advances past them; legacy-path tombstones (seq 0) block re-creation of resurrected records for 30 days
- P1: initial-sync _transformRecord inflates local syncVersion=cloud+1 → every incremental update misclassified as conflict → _sync_conflicts grows unbounded, resolution UI not wired
- Coverage gaps: SmsSettings, PaymentSettings, GlobalAnnouncement, PlanFeature, Favorite, FAQ absent from initial-data stages (never imported fresh); AuditLog written locally but never synced
- Dead code: cloud-sync.js, local-db.js, local-api-routes.js (still packaged), web lib/sync-service.ts, web offline-queue.ts, desktop sync-indicator.tsx, both ConflictResolutionDialogs unmounted
- Web has a 2nd WatermelonDB sync engine (db/sync.ts via db/provider.tsx) sharing the same cloud endpoints → cursor contention with desktop; pull response lacks `timestamp` → WDB incremental broken
- UX: InitialSync.tsx granular + resumable (good); sync UI reads stale/wrong field names (sync-status-detail vs /api/sync/status stale module vars); no manual-sync path fires on connectivity restore (process.on('online') dead, preload events unused)

---
Task ID: 2-a
Agent: Explore-parity-auditor
Task: Desktop vs Web component parity deep audit

Work Log:
- Inventoried + content-diffed all files under components/{agency,shared} on both sides: web agency = 43 files, desktop = 36; web shared = 31, desktop = 30. Raw `diff -q`: only agency/subscription/types.ts is byte-identical; after stripping 'use client', 26 more are mechanical (ease `as const`, `import.meta.env` swaps, `<style jsx>`→`<style>`, `;;` cleanup)
- agency-devices.tsx + devices/* (7 files) MISSING-IN-DESKTOP — verified zero `/api/agency*device*|tv|printer|cast` routes in apps/desktop/local-api/index.js (8305 lines) and zero device UI in desktop App.tsx/Sidebar/pages. BUT the web side is ALSO dead: `AgencyDevices` (agency-devices.tsx:78) has no importer, no ViewRouter case in apps/web/src/app/page.tsx, no /agency/devices route; cloud API has the routes (apps/api/src/routes/agency-devices.ts:391) — UI never wired
- Shadow-file bug CONFIRMED on desktop: hooks/use-realtime.ts (98-line stub) + use-realtime.tsx (137-line v2 stub) both exist; 4 imports use extensionless '@/hooks/use-realtime' (agency-dashboard.tsx:72, agency-fullscreen.tsx:4, dashboard/SimpleMobileDashboard.tsx:5, shared/connection-status.tsx:5); vite.config.ts sets no resolve.extensions → Vite default puts .ts before .tsx → the .ts stub wins. Deleting .ts alone would crash agency-dashboard (desktop .tsx stub lacks onQueueNoShow/Cancelled/Joined/WalkIn/Paused/Resumed/PositionChanged/onNotification/onYourTurn/onTurnApproaching/joinCustomer). Web fixed this same bug in Task 2 by deleting its .ts
- stats-card triple-dead: web stats-card.tsx, desktop stats-card.tsx (legacy simple variant) and desktop stats-card.web.tsx (exact copy of web's) have ZERO importers on either platform
- Shared components dead on BOTH: QuickStatsWidget, queue-status-widget, QueueE2ETestPanel (web comment "removed", desktop copy backed by no-op e2e stub), platform-download-section, DarkModeShowcase. Dead on WEB only: offline-sync-indicator, offline-diagnosis-panel, lan-discovery-panel (zero consumers each — desktop "missing" them is moot)
- Sync UI: web offline-sync-indicator.tsx (238-line banner, WatermelonDB, i18n, auto-sync) is unmounted on web; desktop ships 29-line sync-indicator.tsx + 211-line sync-status-detail.tsx (Sidebar.tsx:210) with hardcoded English strings, zero t() calls — Arabic-first RTL product ships English-only sync UI on desktop
- Realtime: web has full Socket.IO (hooks/use-realtime.tsx 816 lines + lib/realtime.ts); desktop has polling-only no-op stubs — desktop agency-dashboard subscribes 9 onQueue* handlers that are all no-ops
- lib: byte-identical = date-utils, enums, queue-scheduler, sounds, turn-alert-sleep + all 6 adapters/*.ts; api-client.ts = web 740-line class vs desktop 143-line shim whose isApiUnreachable()/isBothUnreachable() hardcode `return false` (desktop:132-141); rbac.ts desktop getAllowedResources() only supports 'read' (web supports read|write|delete, rbac.ts:209-219); desktop route-map.ts:28 maps 'agency-devices' → '/agency/devices' but App.tsx has no such Route (orphan)
- Page-level: 8 desktop pages are 5-line wrappers around shared agency components (Dashboard/Staff/Settings/Subscription/Reviews/Profile/Branches/QrCode) → parity by construction; desktop-native pages Queue.tsx (409, wraps QueueControls+WaitingList+CounterManagement+QueueTimeline+walk-in), Analytics.tsx (386, wraps NoShow/PeakHours/WaitTime/Rating/ServiceBreakdown), History.tsx (203, uses desktop-only AgencyHistoryContent export + ExportCSVDialog — CSV export is desktop-ONLY, web has none), Notifications.tsx (1126), Services.tsx (823)
- Services.tsx DATA-LOSS BUG: sends estimatedWaitMinutes/capacity/description/allowWalkIn/autoComplete (Services.tsx:209-220) but local-api POST /api/agency/services (index.js:4013-4030) persists only name/nameAr/nameFr/prefix/isActive and PATCH (index.js:5688) allows only name/prefix/description/isActive — 5 form fields silently dropped; web's settings-based service CRUD (agency-settings.tsx handleSaveService) sends only name/nameAr/nameFr/prefix = matches backends (cloud apps/api/src/routes/services.ts persists the same 4)
- Offline architecture: web = WatermelonDB+LokiJS in src/db/* but the entire stack is dormant (use-observable-query, lib/sync-service.ts, lib/sw-register.ts have zero consumers; use-offline-sync's only consumer is the unmounted offline-sync-indicator) — web live behavior is api-client 30s unreachable-cooldown; desktop = live Hono local API (127.0.0.1:3080, SQLite) + local-api/sync-service.js + stores/sync.ts + stores/auth.ts (offline login, offline-token expiry) + /initial-sync route + InitialSync page
- Desktop-only features web lacks: InitialSync page, sync-status-detail dialog, History CSV export, per-service capacity/walkIn/autoComplete form (UI only — API drops them), Ctrl+Q shortcut labels (Sidebar.tsx:30,130 — but use-keyboard.ts useKeyboard() has zero consumers, so no shortcut handler is actually registered: decorative)
- i18n: desktop adds 37 keys (identical sets in en/ar/fr) for its exclusive pages; ALL web keys present on desktop (17 web duplicates commented out "DUPLICATE REMOVED" — verified later occurrence kept). Keys used-but-missing: 14 keys missing on BOTH platforms (selectCounter, syncConflictsTitle/Desc, keepLocal/keepServer, local/server, localVersion/serverVersion, noConflicts, resolveAllLocal/resolveAllServer, resolved, done — used by conflict-resolution-dialog.tsx:129-283 + agency-fullscreen.tsx:808; all have inline EN fallbacks); desktop-only misses: fromDate/toDate (pages/History.tsx:107 ExportCSVDialog); web-only misses: customerName (agency-fullscreen.tsx:1167; desktop added it at en.ts:2723 but never backported) + hideQR + 35 dmCast* keys (used only by dead devices/cast-dialog.tsx)
- Desktop use-notifications.ts sets error 'Failed to load notifications' (hardcoded EN) where web uses Arabic 'فشل تحميل الإشعارات'; Sidebar nav labels + SyncIndicator/SyncStatusDetail hardcoded English, no RTL
- Verdict on DESKTOP_WEB_COMPONENT_PARITY.md: its "40 files, 0 divergent" claim is FALSE on three counts: (1) the inventory is wrong (7 device files missing on desktop, unacknowledged), (2) agency-history-sheet.tsx is genuinely divergent (desktop superset: AgencyHistoryContent split, POSTPONED status, detail Eye view), (3) dashboard/types.ts + agency-fullscreen-history.tsx carry real desktop-only logic changes; shared/ components were never covered by that report at all

Stage Summary:
- Parity is real but narrower than claimed: 26 agency + 19 shared files are true mechanical diffs; agency-history-sheet.tsx, dashboard/types.ts, agency-fullscreen-history.tsx are genuinely divergent (desktop = superset, web should adopt POSTPONED + getServiceName fallbacks)
- agency-devices.tsx + devices/* (7 files): missing on desktop AND unreachable on web — decide: wire it into web ViewRouter + port to desktop local API, or delete from both (currently pure dead weight; cloud API routes exist)
- Desktop use-realtime.ts shadows use-realtime.tsx (same bug web fixed in Task 2); must MERGE stubs (tsx lacks 12 handlers used by agency-dashboard) — deleting .ts alone breaks the Dashboard
- P0: desktop Services page silently loses estimatedWaitMinutes/capacity/allowWalkIn/autoComplete/description — local-api POST/PATCH + Prisma schema don't support them
- Dead code everywhere: stats-card ×3, QuickStatsWidget, queue-status-widget, QueueE2ETestPanel+e2e stub, platform-download-section, DarkModeShowcase, use-keyboard (desktop), use-observable-query + lib/sync-service + sw-register + whole web WatermelonDB db/* chain (dormant), offline-diagnosis-panel, lan-discovery-panel
- Sync UI is NOT at parity: desktop's 29-line pill has no i18n/RTL/banner/auto-sync vs web's 238-line banner (which web itself never mounts)
- i18n: 14 keys missing on BOTH platforms (conflict dialog + fullscreen), 2 on desktop only (fromDate/toDate), 1 on web only (customerName); desktop notifications error string hardcoded English
- Desktop rbac.ts getAllowedResources() regressed to read-only vs web's read|write|delete
- Desktop-only wins: InitialSync flow, offline login/auth store, sync status dialog, History CSV export; Web-only wins: full Socket.IO realtime, dev-debug-hud, NotificationCenter + onboarding-wizard actually mounted, LAN/cloud reachability detection
- Recommended source of truth: shared agency components (desktop copies are supersets where they diverge); API shapes: cloud apps/api (desktop local-api must catch up on services fields + device routes if device UI is ever wired)

---
Task ID: 2-d
Agent: Explore-localfirst-auditor-v2
Task: Local-first architecture + UX audit (retry, focused)
Work Log:
- Read main.js boot path (whenReady:1388 → createWindow:389 → loadLoadingScreen:657 → diagnostics gate:1430) and loading-screen.js runDiagnostics (814-999) + finalize/launch UI (524-558)
- Traced local API start: lazily started inside diagnostics step 1 (loading-screen.js:842-948), not in main.js boot; loadApp probes 127.0.0.1:3080 with 5s timeout + 1 retry (main.js:589-648)
- Read local-api/lib/db.js fully: DB path env BLASTI_LOCAL_DB_DIR else ~/.blasti/local (431-442), prisma db push --accept-data-loss at module load (464-528), --force-reset wipe on duplicate-column drift (496-514), WAL pragmas (555-567), no backup/export
- Verified security: BIND_ADDRESS 127.0.0.1 (index.js:39, 8164), blasti-auth.json plaintext JWT (main.js:1098), contextIsolation true / nodeIntegration false / sandbox false (main.js:399-404), local-db:* IPC accepts arbitrary table (main.js:1329-1372)
- Mapped offline flow: local auth fallback bcrypt/scrypt/sha256 (index.js:1060-1139), OFFLINE_TOKEN_MAX_MS=3d (99), mutations write SQLite directly + logPendingMutation (index.js:1900-1912), subscription served locally (4616-4705)
- Read stores/auth.ts, api/client.ts, pages/Login.tsx, App.tsx; grepped use-keyboard/Sidebar/OnboardingWizard/ConnectionStatus/ErrorBoundary/SyncIndicator usage (all defined, mostly unwired)
- Found dead ends: error banner blocks launch with no retry/force-launch (loading-screen.js:541-557); loading:finish gated on allPassed (main.js:695-701) contradicts catch-path promise (main.js:1444-1450); error-page Retry uses location.reload() on data: URL = no-op (main.js:564)
- Found auth bridge bug: setCloudSyncAuth(result.token, user) passes 2 args to preload expecting 1 object (auth.ts:86 vs preload.js:240)
- Found 'blasti:offline-expired' event dispatched (client.ts:104, api-fetch.ts:58) with no listener anywhere; offlineWarning/loginSource stored but never rendered
Stage Summary:
- Boot order OK but local API start lives in diagnostics; port 3080 occupied by a 200-responder is blindly trusted, else hard-blocked with no fallback port
- Any diagnostics error = permanent dead-end window: error banner has no Retry/Force-launch, and crash path sets allPassed=false which blocks loading:finish
- loadApp error page Retry button is a no-op (location.reload on data: URL); existing offline:retry IPC is never used
- DB: no backup/export/reset surface; schema drift auto-wipes local DB via --force-reset without prompt; production packaging lacks prisma schema/CLI so first-run table creation will fail
- Offline login works (local hash verify) but 3-day offline token countdown is invisible (console.warn only); after expiry user is dumped to /login with no reconnect dialog
- Offline writes (queue/staff/branch/service) go straight to SQLite and succeed — good — but no pending-mutation feedback in UI (SyncIndicator orphaned)
- Security solid on bind/CORS/contextIsolation; blasti-auth.json plaintext JWT; sandbox:false; local-db:* IPC over-broad (arbitrary table reads)
- Keyboard shortcuts fake: use-keyboard.ts never imported; Sidebar shows Ctrl+Q label that does nothing
- OnboardingWizard, ConnectionStatus, ErrorBoundary, SyncIndicator all built but not wired into App/AppLayout
- Login.tsx "cloud available" indicator actually probes the local API health, mislabeled; offline button variant exists

---
Task ID: 3-c
Agent: fullstack-frontend-parity-fixer
Task: Desktop/Web frontend parity, i18n, sync UX wiring

Work Log:
- FIX 1 (P0): Merged desktop use-realtime shadow-file pair into ONE file. Kept hooks/use-realtime.ts (Vite resolves .ts before .tsx, so .ts was the live module; all 4 consumers import extensionless → zero consumer edits), DELETED hooks/use-realtime.tsx. use-realtime.ts:1-137 now exposes the union API surface: useRealtime() with connected/isConnected/connectionStatus, joinRoom/leaveRoom, joinAgency/leaveAgency, joinCustomer/leaveCustomer, joinKiosk/leaveKiosk, joinAdmin/leaveAdmin, on/off/emit, onQueue{Created,Updated,Called,Completed,NoShow,Cancelled,Joined,WalkIn,Paused,Resumed,PositionChanged,SettingsUpdated}, onReservation{Created,Updated,Cancelled}, onNotification, onTurnApproaching, onYourTurn, onKioskUpdate, onAgencyUpdated, onStaffUpdated, subscribe/unsubscribe/onAnyEvent; plus useAgencyRealtime(), useCustomerRealtime(), useTurnAlert() (from the dead .tsx) and types ConnectionStatus/RealtimeEventData. Verified against all call sites (agency-dashboard.tsx:663-671 needs onQueueNoShow/Cancelled/Joined/WalkIn/Paused/Resumed/PositionChanged; agency-fullscreen.tsx:550-566 needs onReservationCreated/Updated + onQueueUpdated; SimpleMobileDashboard.tsx:258-295; connection-status.tsx:148,253 needs isConnected/connectionStatus). Desktop transport reality preserved: polling-only no-ops against local API 127.0.0.1:3080 — no socket stack ported.
- FIX 2 (P1): apps/desktop/frontend/src/lib/rbac.ts:206-223 — getAllowedResources(role, action='read'|'write'|'delete') now matches web's lib/rbac.ts:211-221 (was read-only); default 'read' keeps any positional callers safe; grep confirmed zero desktop callers, so no breakage.
- FIX 3 (P1): apps/web/src/components/agency/agency-history-sheet.tsx:95-103 STATUS_OPTIONS + :130-133 STATUS_MAP — backported POSTPONED ('statusPostponed' label + purple badge classes) exactly as desktop's agency-history-sheet.tsx:97-136; 'use client' + structure/export signature preserved; did NOT port AgencyHistoryContent split.
- FIX 4 (P1): apps/web/src/components/agency/agency-fullscreen-history.tsx:132-136 — getServiceName now uses desktop's fallback chain (serviceName||name, serviceNameAr||nameAr, serviceNameFr||nameFr).
- FIX 5 (P1): i18n keys added to BOTH platforms' flat dictionaries (en/ar/fr): selectCounter, syncConflictsTitle, syncConflictsDesc, keepLocal, keepServer, local, server, localVersion, serverVersion, noConflicts, resolveAllLocal, resolveAllServer, resolved, done. Web additionally: customerName ('اسم العميل' to match dictionary tone; desktop already had it) + statusPostponed (required by FIX 3; desktop already had it). Desktop additionally: fromDate, toDate (History CSV page). Grep-verified no pre-existing key collisions; desktop-only pre-existing inEnglish duplicate (ar:1428/en:1426/fr:1416 vs later occurrence) commented out with 'DUPLICATE REMOVED (3-c):' marker keeping runtime value (later occurrence) unchanged.
- FIX 6 (P1): stores/sync.ts:118-168 — mapped FULL /api/sync/status contract (isSyncing, pendingMutationsCount, abandonedMutationsCount, conflictsCount, lastSuccessfulSync, lastCloudContactAt, offlineTokenRemainingMs, syncProtocolVersion, syncModelCount, syncError) while keeping legacy fields lastSyncAt/lastError/pendingMutations/isOnline (used by use-offline-aware-polling.ts:52,62, use-notifications.ts:88, Sidebar). Added conflicts: SyncConflictItem[] + conflictsLoading + refreshConflicts() (stores/sync.ts:213-236) + resolveConflict() (stores/sync.ts:237-262) with tolerant row normalizer (modelName/cloudData/createdAt spellings from local-api/sync-service.js _sync_conflicts table) and typed SyncElectronBridge (no any).
- FIX 6 wiring: preload.js:365,372 DOES expose getSyncConflicts/resolveSyncConflict (IPC sync:conflicts / sync:resolve-conflict → main.js:1307-1324 → syncService.getConflicts/resolveConflict) — refreshConflicts prefers the bridge, falls back to apiFetch GET /api/sync/conflicts (route does NOT exist yet in local-api/index.js:5390-5432 — fallback kept, dialog closed-by-default, resolve fallback POSTs /api/sync/conflicts/resolve non-fatally). NOTE for 3-b: main.js sync:conflicts handler calls syncService.getConflicts()/resolveConflict() which do NOT exist in sync-service.js yet (handler try/catch returns []).
- FIX 6 UI: sync-status-detail.tsx rewritten — displays humanized last successful sync + last attempt (i18n justNow/secondsAgo/minutesAgo/hoursAgo/daysAgo/never), pending count, abandoned count (ArchiveX), conflicts count, sync error box, offline-token remaining ('2d 4h' format via formatRemainingMs), protocol version (GitBranch); Force Sync Now + Close kept, all strings now t(). sync-indicator.tsx: i18n via useLanguage (syncOnline/syncOffline/syncSyncing/pendingChanges/syncErrorLabel), truncate-safe pill. Sidebar.tsx: SyncIndicator mounted in brand header (:121), ConflictResolutionDialog mounted closed-by-default (:243-259) wired to store with onResolve/onResolveAll → useSync.resolveConflict, conflicts entry-point button shown only when conflictsCount>0 (:223-234), sync status block fully i18n'd (allSynced/syncSyncing/pendingChanges/syncErrorLabel/syncOffline/allDataUpToDate/changesAwaitingSync/syncLastSync/noSyncData/retrySync/syncNow + queueManagement brand subtitle); text-left→text-start for RTL.
- FIX 7 (P2): Sidebar.tsx:28-42 nav labels → t(labelKey) using existing keys (dashboard/services/branches/history/analytics/subscription/reviews/profile/settings) + 4 new keys where missing (queue/staff/qrCode/alerts).
- Verification: desktop frontend bunx tsc --noEmit has NO node_modules installed in sandbox → only TS2307/ImportMeta.env environment errors + pre-existing Analytics.tsx/calendar.tsx/input-otp.tsx errors remain; ZERO errors in files touched here (i18n TS1117 dup errors cleared by inEnglish dedupe). Web: eslint on the 5 touched web files = 0 problems; full web tsc error count dropped 301→285 (16 pre-existing missing-i18n-key errors FIXED: conflict-resolution-dialog.tsx + agency-fullscreen.tsx + agency-fullscreen-history.tsx), 0 new. Incident: a git stash round-trip to capture a web-tsc baseline raced with a concurrent agent editing apps/desktop/local-api/index.js — all 3-c files restored intact from the stash via git checkout stash@{0} -- <paths>, stash dropped; local-api/index.js + packages/cloud-db/index.ts (other agents' files) left untouched.

Stage Summary:
- Desktop use-realtime is now ONE file (.ts survivor, .tsx deleted) with the complete merged API surface; the 12 missing onQueue* handlers agency-dashboard calls exist at runtime (guarded no-op noopSub pattern preserved — desktop stays polling-only, no socket port).
- rbac getAllowedResources supports read|write|delete on desktop (parity with web).
- Web history sheet gains POSTPONED status filter/badge; web fullscreen-history getServiceName has full ar/fr/name fallback chain.
- 16 i18n keys per platform added (desktop: 14 shared + fromDate/toDate + 24 sync-UI/nav keys; web: 14 shared + customerName + statusPostponed); Arabic/French match dictionary tone (Arabic-first RTL).
- Sync UX wired end-to-end on desktop: store maps the exact /api/sync/status contract + conflicts array via preload bridge (sync:conflicts IPC, local-API fallback), SyncIndicator in sidebar header, ConflictResolutionDialog closed-by-default with conflicts entry point, SyncStatusDetail shows all contract fields incl. offline-token '2d 4h' and protocol version — zero hardcoded English left in sync UI.
- Blocked-on-3-b note: sync-service.js must export getConflicts()/resolveConflict() for the IPC bridge to return data; local API lacks GET /api/sync/conflicts and POST /api/sync/conflicts/resolve routes (UI degrades gracefully: empty state via noConflicts).
- Skipped: porting web's socket.io stack to desktop (out of scope by design), AgencyHistoryContent split (explicitly excluded), dmCast* keys (dead code), web Sidebar (not in scope).

---
Task ID: 3-a/3-b/3-c (consolidated closeout)
Agent: main (Z.ai Code)
Task: Complete the 4-dimension deep audit (desktop↔web parity, cloud↔local API, sync engine, local-first) and implement all P0/P1 fixes

Work Log:
- 2-d retry (Explore) delivered local-first audit: startup/dead-end paths, DB lifecycle (silent force-reset wipe), offline workflow matrix, security
- Completed 3-a remainder myself after agent timeout: defined verifyQRTokenLocal (was USED-but-UNDEFINED crash bug), fixed POST /api/reservations 500 (counter.count agencyId → branch relation) + in-tx capacity re-check, dashboard activeCounters permanent-[] stub, counter delete activeReservationId→currentReservationId, counter create/PATCH branchId+staffId validation, analytics/CSV/stats waitTime aggregates (no Reservation.waitTime column), postpone 3-step shift wrapped in $transaction, requireActiveSubscription on queue-status, user profile PATCH extra fields, truthful /api/sync/status + new /api/sync/conflicts(+resolve) routes
- Trimmed desktop Services.tsx fictional form fields (estimatedWaitMinutes/capacity/allowWalkIn/autoComplete not Service model fields)
- Completed 3-b remainder myself: sync.ts client-owned pull cursor contract (sinceSequence accepted, shared cursor untouched, cursor/sequences/timestamp in response, DeletedRecord supplement legacy-only), sync-service push results parsing + _lastPushedVersion advance + syncedAt stamping, LOW_FREQUENCY skip removed, sequence-aware tombstones + resurrection guard, initial-sync syncVersion inflation fixed, syncedAt-based conflict rule (echo-of-own-push convergence), pull pagination (≤10 pages), sync-auth-required pause on 401/403, WAL replay syncedAt stamping helper, web db/sync.ts WDB model filter + timestamp passthrough
- Found+fixed during verification: (1) Reservation/AgencyStaff had NO updatedAt in either schema — cloud hydration 500'd and desktop queue ops would crash on write → added DateTime? @updatedAt to both schemas, db:push additive; (2) SyncChange hook awaited inside interactive tx → SQLite write-lock deadlock (P2028 on walk-in, 5000ms busy timeout) → fire-and-forget; (3) stale SQLite file descriptor after earlier db:push → API restart via daemon-start.sh (double-fork, persists in sandbox); (4) auth rate-limit tripped by repeated curl logins → cleared via restart
- Discovered+fixed web-bug: cloud /api/agency/stats,/queue,/announcements required ?agencyId= and web dashboard never sent it (400s) → session-agency fallback via resolveUserAgencyId mirroring /analytics; verified 200 live
- Browser E2E (agent-browser): landing ar-RTL → owner1 login → agency dashboard live (يُخدم الآن 7، 3 في الانتظار، متصل بالخادم، آخر مزامنة 28s)، queue actions، CSV export؛ 0 page errors / 0 console errors
- Verified: node --check all modified JS; tsc apps/api (0 new errors), packages/cloud-db clean; web lint clean on touched files; pull contract live (sinceSequence/cursor/sequences/timestamp); D2 E2E walk-in C-008 delivered via pull with real sequences

Stage Summary:
- All four audit dimensions now have accurate reports (prior parity docs' claims corrected in PARITY_SYNC_AUDIT.md)
- Sync engine contract is now client-owned-cursor + fed by cloud business writes (D1/D2/D3 fixed + proven E2E)
- Desktop local API: no more poison-pill WAL entries, no more 500s on reservations/counters/analytics, HMAC QR verification, per-service numbering, subscription enforcement closed gaps
- Web dashboard 400s fixed; realtime handlers complete on desktop; i18n/RTL sync UI wired with conflict resolution
- Remaining P2s + desktop-only P0s (loading-screen dead-end, DB-wipe backup, setCloudSyncAuth call shape, packaging) documented in PARITY_SYNC_AUDIT.md §4.2/§5 for a dedicated Electron session
---
Task ID: 4-c
Agent: web-parity-fixer (devices wiring + dead-code purge)
Task: Wire agency-devices into web ViewRouter/nav, purge zero-importer dead code (web+desktop), remove cloud duplicate route registrations
Work Log:
- A. Devices wiring (PARITY_SYNC_AUDIT §5-1): apps/web/src/app/page.tsx:33 added lazy `AgencyDevices` import + :130-131 `case 'agency-devices': return <AgencyDevices />` (component takes no props — reads user/agencyId from use-app-store, matching neighboring agency cases). apps/web/src/components/platform/adaptive-sidebar.tsx:37 added `Monitor` icon import, :171 nav item `{ view: 'agency-devices', icon: Monitor, label: t('devices') }` after agency-branches; agency nav is a manually enumerated array with NO role gating on any item (subscription/settings included) and cloud GET /api/agency-devices only requires auth+agencyId (agency-devices.ts:1695-1727), so no gating added.
- A-i18n: added `devices` key (en 'Devices' i18n/en.ts:1322, ar 'الأجهزة' i18n/ar.ts:1279 Arabic-first RTL, fr 'Appareils' i18n/fr.ts:1280) inserted after `branchesCounters` in each flat dictionary; rg-verified exactly 1 occurrence of `^\s+devices:` per file (no duplicate-key regression). TranslationKeys derives from ar.ts so t('devices') typechecks.
- A-discovery paths: use-discovery-service.ts calls /api/agency-devices/discovery/{health,devices,scan/start,scan/stop,scan/status,protocols,diagnostics} — ALL exist as embedded handlers in apps/api/src/routes/agency-devices.ts:2735-2830 (mode:'embedded', no port-3010 mini-service needed); mounted at /api/agency-devices (apps/api/src/index.ts:391). next.config.ts rewrite `/api/:path*`→:3003 covers the whole surface. REPORTED path mismatch (not fixed): agency-devices.tsx:470 tests unpaired printers via `/api/probe?XTransformPort=3010` — no /api/probe route exists on cloud (discovery is embedded) and no 3010 service exists; direct-dev requests 404 → the test button shows 'offline'. Left as-is (needs a cloud /api/probe or removal — needs product decision).
- A-cast-dialog decision: apps/web/src/components/agency/devices/cast-dialog.tsx calls /discovery/cast/protocols, /discovery/cast/:proto, /discovery/cast/stop (cast-dialog.tsx:113,211-212,278) — NONE exist on cloud (only POST /discovery/cast, agency-devices.ts:2997). The live casting flow is the inline one-shot handleCastTv → POST /discovery/cast + toasts (agency-devices.tsx:395-426). cast-dialog is NOT the intended UI for the current API surface → NOT wired; deleted as dead code.
- B. Dead code purge (§5-2) — every file rg-verified ZERO importers across apps/web/src and apps/desktop/frontend/src (module-name + exported-symbol searches, barrel files checked: none exist; QueueE2ETestPanel's only web ref is the removal comment page.tsx:64). DELETED web (12): components/shared/{stats-card,DarkModeShowcase,QueueE2ETestPanel,lan-discovery-panel,offline-diagnosis-panel,offline-sync-indicator,platform-download-section}.tsx, components/agency/{QuickStatsWidget,queue-status-widget}.tsx, components/agency/devices/cast-dialog.tsx, hooks/use-offline-sync.ts (its ONLY consumer was offline-sync-indicator.tsx:9,84 — verified, then deleted too), lib/e2e-queue-test.ts (zero importers; exclusive stub of the deleted web panel; carried 8 pre-existing tsc errors). DELETED desktop (8): components/shared/{stats-card,stats-card.web,DarkModeShowcase,QueueE2ETestPanel,platform-download-section}.tsx, components/agency/{QuickStatsWidget,queue-status-widget}.tsx, lib/e2e-queue-test.ts (only imported by the deleted desktop panel). Desktop platform-download-section checked against electron download flow FIRST: apps/desktop/main.js has zero 'download' references — no flow references it. Did NOT touch src/db/* WatermelonDB stack, lib/sync-service.ts, lib/sw-register.ts, use-observable-query (dormant-by-design), desktop Electron main process, local-api.
- B-verification: apps/web `bunx tsc --noEmit` = 243 errors / 308 output lines. Pre-my-change tree measured in-session at 251 errors / 321 lines (the recorded 285 line-baseline drifted from prior-round/concurrent edits BEFORE my session — apps/web vs HEAD contains only my edits + a chmod-only dev-debug-hud.tsx, so pre-session tree already exceeded 285 lines). My net effect: −8 errors (e2e-queue-test.ts), zero errors in touched files, zero TS2307 for any deleted path (only pre-existing i18next TS2307 remains). ESLint: full run = 39 errors + 790 warnings, ALL 39 in pre-built bundle public/desktop/assets/index-Cyro0GdQ.js (documented pre-existing); zero problems in changed files.
- C. Cloud duplicates (§2.3): line-by-line diff of agency.ts set-1 (3830-4172: GET /hardware, GET+POST /hardware/orders, POST+GET /enterprise-request, POST /subscription/cancel) vs set-2 (4179-4406). Set-1 is a superset everywhere: zod validateBody with detailed 400s vs loose manual checks, batch product fetch + tier.isActive via findFirst vs N+1 findUnique, $transaction for order+audit vs two separate writes, richer audit details (agencyId/itemCount/previousTier/previousStatus vs {}). Set-2 uniques considered for merge: (1) disabled-GET /hardware payload `upfrontDiscount: 0` — cosmetic only (UI hides the section when disabled), skipped; (2) lenient string-quantity parseInt — web UI (agency-subscription.tsx:607-620) sends numeric quantities matching set-1's z.number().int() schema, skipped; (3) whitespace-only message trim check — zod min(1)+email already covers the UI contract, skipped (documented). Set-2 registers NO route absent from set-1 → DELETED lines 4174-4407 wholesale (git diff: 234 deletions, 0 insertions). `bunx tsc --noEmit`: current tree 238 errors (baseline drifted from another agent's concurrent apps/api edits — initial-sync.ts modified outside this task); ALL agency.ts errors sit at lines <4174 in code untouched by the deletion → zero new errors from this change.
- D. E2E probe (no browser): seed creds from packages/db/prisma/seed.ts:466 (owner1/owner123). POST /api/auth/login via dev-server proxy → 200 + token. All 9 endpoints the wired view calls returned 200 through http://127.0.0.1:3000 with Bearer token: /api/agency-devices (devices list incl. seeded device), /discovery/health ('Embedded multi-protocol discovery active'), /discovery/devices, /discovery/scan/status, /discovery/protocols, /discovery/diagnostics, /discovery/saved-tvs, /discovery/default-printer, /unpaired. (First probe round 401'd — self-inflicted truncated token, re-verified with full token.) agency-devices also confirmed NOT needing page.tsx special-casing (only agency-fullscreen/-history render outside ViewRouter, page.tsx:432-443).
Stage Summary:
- Agency-devices is now reachable end-to-end on web: hash route #/agency/devices → ViewRouter case → lazy AgencyDevices; sidebar nav entry (Monitor icon, t('devices')=الأجهزة/Appareils/Devices) added with no new role gating (matches existing agency nav + cloud auth model).
- Discovery contract verified live: all 7 discovery endpoints + device CRUD/pair/unpair/command/kiosk-cred routes the UI calls exist on cloud and return 200 through the Next proxy; discovery is embedded (no 3010 dependency). One reported mismatch: /api/probe?XTransformPort=3010 printer-test call has no cloud route (pre-existing; needs decision).
- cast-dialog deleted rather than wired — it targets a protocol-specific cast API (/cast/protocols, /cast/:proto, /cast/stop) that does not exist server-side; live casting is the inline one-shot POST /discovery/cast.
- 20 dead files purged (12 web + 8 desktop) with zero-importer proof each; use-offline-sync and both e2e-queue-test stubs removed with their sole consumers; web tsc errors decreased (251→243), lint unchanged (39 pre-existing bundle errors), API tsc: zero new errors from the 234-line duplicate-route deletion.
- Cloud agency.ts duplicate registrations gone: first (live, richer) set kept verbatim; three minor set-2 deltas evaluated and consciously not merged (cosmetic/lenient/edge-case, documented above).

---
Task ID: 4-a
Agent: Desktop-Electron-local-first-fixer
Task: Fix all unresolved Electron local-first P0/P1/P2s from PARITY_SYNC_AUDIT.md §4.2 (loading-screen dead-end, error-page no-op retry, DB wipe without backup, setCloudSyncAuth shape, offline-expired UX, sync pill warning, packaging gaps, port identity, IPC allowlist, keyboard shortcut)
Work Log:
- Read worklog.md (last 150+ lines) — confirmed 2-d/3-a/3-b/3-c scope; did NOT redo their work (realtime merge, rbac, i18n sync keys, sync contract)
- FIX A1 (P0-1) loading-screen.js: replaced no-op `onclick="location.reload()"` error-banner retry with "إعادة الفحص" (retry diagnostics) → new retryDiagnostics() (loading-screen.js:564-572) which resets page UI (resetDiagnosticsUI :541-562 zeroes pass/warn/fail counters, allDone flag, step cards, progress bar, hides banner/launch areas) then calls new preload bridge diagnosticsRerun() (preload.js:337-339 → ipcRenderer.send('diagnostics:rerun')); location.reload() kept only as inert fallback when bridge missing. Added emerald secondary "تشغيل على أي حال" button (.force-btn CSS :362-371) calling existing finishLoading()
- FIX A2 (P0-1) main.js: added ipcMain.on('diagnostics:rerun') (:723-746) re-running runDiagnostics(mainWindow, diagnosticsConfig) — config captured at whenReady (:1519-1521) so rerun uses identical cloudBaseUrl/userDataPath; results stream back through the SAME 'diagnostics:update'/'diagnostics:finalized' channels the original run uses; recompute diagnosticsDone/diagnosticsAllPassed; guarded by diagnosticsRunning flag (set around initial run :1538/:1540-1543 and rerun :748/:762) to prevent concurrent flows
- FIX A3 (P0-1) main.js loading:finish (:705-715): now refused ONLY while diagnosticsRunning; when diagnosticsDone && !diagnosticsAllPassed the explicit user click sets forceLaunch=true (logged as FORCE LAUNCH) and proceeds to loadApp — the "Launch manually" promise is finally kept
- FIX A4 (P0-2) main.js buildErrorPage (:539-576): error page is Arabic-first RTL (lang="ar" dir="rtl"); Retry button = `(window.electronAPI && window.electronAPI.retryOffline) ? window.electronAPI.retryOffline() : location.reload()` — preload IS attached to data: URLs (same webPreferences; proven by the loading screen's own electronAPI usage, and by the pre-existing offline fallback page at main.js:280-293 which already used this exact pattern), so retryOffline → 'offline:retry' (:872-874) → loadApp() works; no new IPC needed. Buttons: "إعادة المحاولة" / "إغلاق"
- FIX B (P0-3) local-api/lib/db.js: new backupLocalDb() (:497-515) copies DB + -wal/-shm sidecars to `<dbfile>.bak-<ISO-slug>` (constant-length slug ⇒ lexicographic==chronological) and pruneLocalDbBackups() (:522-540) keeps the 3 newest (+sidecars); duplicate-column force-reset branch (:593-621) now calls backupLocalDb('pre-force-reset') FIRST and on backup failure ABORTS the reset and RETHROWS ("Refusing to force-reset local DB: backup failed") → module-load try/catch logs it loudly; DB is never wiped without a successful backup. Backup path is console.logged
- FIX C (P0-4) stores/auth.ts:88: setCloudSyncAuth(result.token, user) → setCloudSyncAuth({ token: result.token, user }) — matches preload.js:240 (single params object) and main.js 'cloud-sync:set-auth' handler which destructures { token, user } (main.js:1121); grep: only this one call site
- FIX D (P1-5) App.tsx: added useEffect listener for 'blasti:offline-expired' (:54-61) fed by api/client.ts:104 + lib/api-fetch.ts:57 (detail = { offlineDays, message }); renders Arabic-first RTL modal overlay (:127-148) with i18n keys offlineExpiredTitle/Desc/Action, desc interpolates {days} from detail.offlineDays (fallback 3 = OFFLINE_TOKEN_MAX_MS); action button clears the stale session via useAuth.getState().logout() then <Navigate to="/login" replace> (prevents /login→"/" bounce while isAuthenticated is stale). Keys added to ALL 3 dictionaries (en.ts/ar.ts/fr.ts:2778-2780) with Arabic + French translations
- FIX E (P1-6) sync-indicator.tsx: reads offlineTokenRemainingMs from useSync (field confirmed at stores/sync.ts:117/161/184/208); when isOnline && !syncError && 0 < remaining < 12h (OFFLINE_WARNING_THRESHOLD_MS :7) the pill goes amber (bg-amber-500/10 text-amber-400, AlertTriangle icon :30-42) with tooltip "صلاحية جلسة عدم الاتصال: 2d 4h" via local formatRemainingMs (:10-18)
- FIX F (P1-7) DELETED local-api-routes.js, local-db.js, cloud-sync.js after verifying zero importers (only cloud-sync.js→require('./local-db') internal chain; nothing requires cloud-sync.js/local-api-routes.js; better-sqlite3 used by neither desktop main.js nor local-api). electron-builder.yml rewritten: files list now includes local-api/**/* (was MISSING ENTIRELY — main.js require('./local-api/index') would crash packaged builds), deprecated entries + better-sqlite3 asarUnpack removed with explanatory comment (:22-26); extraResources add exact runtime paths db.js resolves: <resources>/prisma/schema.prisma (from ../../packages/db/prisma/schema.prisma), prisma CLI bundle (from node_modules/.bun/prisma@6.19.3+1fb4c65d43e298b9/node_modules/prisma → prisma/node_modules/prisma), @prisma/config (required by the CLI bundle, verified via rg on build/index.js), @prisma/engines incl. schema-engine binary — mirrored as node_modules layout so require('@prisma/engines') resolves. db.js runtime fallbacks added to match: getSchemaPath() (:53-61) and packaged findPrismaBin() candidate (:197-205) resolve process.resourcesPath/prisma/…, runPrismaCommand cwd falls back to schema dir when packages/db missing (:245-250). yml validated with js-yaml parse
- FIX G (P2-8) port 3080 identity: local-api/index.js /api/health now returns service:'blasti-local' (:860-871). loading-screen.js step 1 (:894-924) parses the health body of an already-running 200-responder and FAILS the step with Arabic error "المنفذ 3080 مشغول بتطبيق آخر…" + [FAIL] log when service!=='blasti-local' && mode!=='local' (backward-compatible: old health shape with mode:'local' still passes); main.js loadApp() probe switched to /api/health with body collection (:603-652) — non-Blasti responder ⇒ clear console.error + error page instead of loading the foreign app. No fallback ports (scope control, per task)
- FIX H (P2-8b) main.js local-db:* allowlist: getAllowedDbTables() (:1397-1419) derives Prisma model delegates from the live client (own property with findMany+findUnique functions; verified 84 delegates detected on Prisma 6.19 client), resolveLocalDbModel() (:1425-1434) validates type + membership; local-db:query/get-by-id/count (:1436-1476) now return { success:false, error:'Unknown table: …' } for anything else (was arbitrary localDb[table])
- FIX I (P2-9) use-keyboard.ts: the reported `}, andleKeyDown]);` syntax bug is NOT present in the current file (line 55 is `}, [handleKeyDown]);`; git shows zero diff — likely stale audit or fixed during the 3-c stash restore); verified with node --check of extracted hook + file re-read. WIRING done: Sidebar.tsx mounts useKeyboard([{ key:'q', ctrl:true, handler: () => navigate('/queue') }]) (:65-67) — the "Ctrl+Q" label on the queue nav item (Sidebar navItems) is now functional; hook's input-field guard + unmount cleanup untouched
- FIX J verification: node --check PASSED on main.js, preload.js, loading-screen.js, local-api/lib/db.js, local-api/index.js; loading-screen inline <script> extracted from generated HTML and node --check PASSED separately (template-literal contents aren't checked by the outer check); generated loading HTML asserted to contain the 2 new buttons + no old location.reload banner; electron-builder.yml parsed via js-yaml (files/asarUnpack/extraResources as intended); i18n: 3 new keys × 3 dictionaries, regex key-scan shows 0 duplicate keys in en/ar/fr and all keys present (offlineExpired* once per file); scripts/dedupe-i18n.cjs run — net file diff vs HEAD limited to the 3 added keys per file; re-read every edited TS file fully (App.tsx, auth.ts, sync-indicator.tsx, Sidebar.tsx, use-keyboard.ts unchanged); tailwind classes used (bg-background/80, backdrop-blur-sm) already established in repo (Notifications.tsx:232)
Stage Summary:
- Loading-screen dead-end is GONE: diagnostics failures now offer "إعادة الفحص" (real main-process rerun with fresh results streamed to the same screen) and "تشغيل على أي حال" (force-launch allowed once diagnostics completed — only still-running diagnostics refuse); forceLaunch flag documented in main.js
- Error page Retry works (retryOffline bridge → loadApp); page is Arabic-first RTL consistent with the product
- Local DB can no longer be wiped silently: force-reset requires a successful timestamped backup (3 kept, sidecars included) or it aborts loudly
- setCloudSyncAuth contract fixed end-to-end (renderer → preload → ipcMain → syncService.setAuth + blasti-auth.json persistence + localApi.setSession)
- Offline expiry UX closed: 'blasti:offline-expired' finally has a listener — Arabic-first modal explains the {days}-day offline window and routes to /login after clearing the stale session; i18n keys in en/ar/fr
- Sync pill turns amber with remaining-time tooltip when offline-token budget < 12h (uses existing offlineTokenRemainingMs from the sync store)
- Packaging: local-api/**/* is now bundled (was missing → guaranteed crash), 3 deprecated files + better-sqlite3 unpack entry removed, prisma schema + CLI + engines shipped as extraResources with matching runtime fallbacks in db.js so first-run `db push`/`generate` can work in packaged builds
- Port 3080 is verified by identity (service:'blasti-local') in BOTH the diagnostics flow and loadApp; a foreign 200-responder fails loudly with an Arabic message instead of being trusted
- local-db:* IPC is allowlisted to real Prisma model delegates (84 tables); unknown tables rejected
- Ctrl+Q shortcut is real (Sidebar-mounted useKeyboard → /queue); use-keyboard.ts syntax confirmed already-correct
- Not done (out of scope per task): fallback ports for occupied 3080; 'local-db:changed' preload listener has no main-process sender (pre-existing orphan, untouched)
---
Task ID: 4-b
Agent: Sync engine enhancement fixer
Task: PARITY_SYNC_AUDIT §5 P2 items 4,5,6,3 + E2E verification of conflicts routes
Work Log:
- A (§5-4 stable idempotency keys): local-api/index.js:578-614 added deriveStableIdempotencyKey() — POST keys are now `POST:<path>:<sha256(normalized body)>` (keys sorted recursively; defensive volatile-field strip list clientTimestamp/clientTime/timestamp/issuedAt/nonce/requestId/localId; createHash was already imported at line 25). Wired into BOTH logging paths: logPendingMutation (index.js:659-664, replaces `${method}:${path}:${freshRowId}`) and the transactional atomicWriteWithMutation (index.js:127-132). Audited all ~90 logPendingMutation call sites: bodies are raw client payloads (generated ticket numbers/displayNumbers/ETA are computed server-side AFTER the body is logged — reservations POST 2654-2671 vs log at 2732; walk-in 4478-4495 vs log at 4535), so no per-site field exclusions were needed. PATCH/DELETE keys unchanged; existing pending rows keep their keys. Documented edge: empty-body action POSTs (pause/resume/toggle-pause/read-all) now dedupe double-taps = correct idempotent semantics; sequential offline call-next with identical body dedupes in the WAL but Reservation state still converges via record-diff push.
- B (§5-5 missing initial-sync stages): apps/api/src/routes/initial-sync.ts — 6 new stages added to STAGE_DEFINITIONS in settings-before-operational order (queueSettings → smsSettings → paymentSettings → reservations → reviews → favorites → faqs → notifications → announcements → globalAnnouncements → transactions → subscriptionPlans → planFeatures), each with a fetchStageData case (initial-sync.ts:224-256, 292-328, 373-386, 414-430), estimateStageCount cases (:659-676), and serializeRecords stageToModel entries (:479-486). Scoping decisions (verified in BOTH prisma schemas — cloud packages/cloud-db:196,562,593,606,686,705; local packages/db:196,551,582,595,670,689): Favorite = agencyId-scoped query + added to PAGINATED_STAGES (:107-113); PlanFeature = scoped via plan:{isActive:true} relation (same exposure class as the pre-existing global subscriptionPlans stage); SMS/PaymentSettings/GlobalAnnouncement/FAQ have NO agency scoping in the schema — implemented as documented global reads with projections mirroring the incremental pull (sync.ts:431-533): SmsSettings projection EXCLUDES the apiKey credential + templates, PaymentSettings projection matches sync.ts and the already-PUBLIC GET /api/payment-settings route (no new exposure), FAQ fetched without isActive filter so locally-deactivated FAQs aren't resurrected. Desktop: initial-sync.js STAGE_MODEL_MAP +37-66 gains the 6 stage→model entries (raw-SQL upsert fallback path verified — all 6 tables exist in packages/db with id unique keys). Header stage list updated to 19 stages.
- C (§5-6 server-clock calibration): sync-service.js:320-340 added clock-offset estimator — on EVERY successful pull page (_pullPageOnce → _calibrateClockOffset(pullData.timestamp) at :932-935): offsetMs = serverTimestampMs - Date.now() (positive = device behind), first sample verbatim then EMA newOffset = 0.5*old + 0.5*sample, clamped to ±7 days, console.warn when |sample| > 60s; persisted in _sync_meta key 'clockOffsetMs'; missing/invalid timestamp → skipped (never throws). In-memory cache _clockOffsetMs hydrated from meta at each _syncCycle start (:1754). Skew-aware LWW in _applyConflictResolution last_write_wins branch (:457-469): cloud stamp converted to device frame (cloudMs - offset) before comparison — byte-identical to old behavior at offset 0. getStatus() now exposes clockOffsetMs + serverTimestamp (:1967-1971) and index.js /api/sync/status surfaces both (:5647-5650). Deliberately NOT skew-adjusted (documented at :457-466): the D7 localModifiedSinceSync gate compares updatedAt vs syncedAt whose frames depend on the last writer (local edit = device frame, cloud ingest = server frame) — single-sided offset correction risks silently dropping genuine local edits (worse failure class than the phantom-conflict noise it would fix); left skew-immune.
- D (§5-3 sync-registry SSOT): DIFFED first — local SYNC_TABLES (sync-service.js:17-37, 19 models) vs packages/core/src/sync-registry.ts isSynced set: IDENTICAL (19/19, zero drift; only ORDER differed — registry declaration order now canonical; registry-order change is safe: pull filtering is order-independent, push loop processes models independently). packages/core has NO build output (main/exports point at .ts), and @blasti/core is not even resolvable from apps/desktop (not a dependency there), so implemented the JSON-artifact option: NEW packages/core/sync-registry.json (generated: _comment header + protocolVersion + syncedModels[19] + per-model delegate/isSynced/isAgencyScoped/conflictStrategy/syncOrder) + NEW generator packages/core/scripts/generate-sync-registry-json.js (parses the .ts, fails loudly on parse drift; run `node packages/core/scripts/generate-sync-registry-json.js` after any sync-registry.ts change) + loud SSOT header in sync-registry.ts:10-21 + "./sync-registry.json" subpath added to packages/core/package.json exports. sync-service.js:17-70 now requires the JSON (relative path → @blasti/core subpath fallback → frozen in-file fallback with startup warning); startup log verified live: "SYNC_TABLES loaded from @blasti/core sync-registry.json (19 models, protocol v2)". AGENCY_SCOPED_TABLES deliberately NOT derived from the registry (documented at :72-77): registry marks Counter "agency-scoped" via branch relation; deriving would break _gatherLocalChanges' branchId sub-query and push other agencies' counters. Also fixed stale syncModelCount (index.js had hardcoded 12): getStatus() now returns SYNC_TABLES.length.
- E (E2E conflicts routes): local API started standalone in sandbox (nohup node -e require('./local-api/index').startLocalApi(); env DATABASE_URL=file:/home/z/my-project/packages/db/data/custom.db NEXTAUTH_SECRET=blast1-dev-... BLASTI_CLOUD_URL=http://localhost:3003; log /tmp/local-api-4b.log; note: sandbox reaps children between commands → full lifecycle must run in ONE command; first attempts also revealed $! captured the backgrounded subshell not node → use `( ... && exec nohup node ... ) &`). Results (owner1/owner123, source=cloud via 3003): GET /api/sync/conflicts no-auth → 401 {"success":false,"error":"Authentication required"}; with auth → 200 {"success":true,"conflicts":[]}; POST /api/sync/conflicts/resolve invalid id → was 500-with-Prisma-text on fresh DBs (E2E FOUND 2 real bugs: (1) _sync_conflicts table not ensured in resolveConflict/getConflicts → "no such table" 500 on fresh local DBs, FIXED with _ensureConflictsTable() in both (sync-service.js:2056-2059, 2087-2089); (2) not-found case returned 500, FIXED via e.notFound tag → 404 (sync-service.js:2092-2097, index.js:5683-5688)); final: 404 {"success":false,"error":"Conflict not found or already resolved: nonexistent-id-123"}; empty body → 400; GET /api/sync/status → {"clockOffsetMs":0,"serverTimestamp":null,"syncProtocolVersion":2,"syncModelCount":19}. Port 3080 was NOT in use before the task; server killed after tests (verified freed, no leftover procs).
- F (verification): node --check ✅ index.js, sync-service.js, initial-sync.js, generate-sync-registry-json.js. tsc apps/api: 238 errors with ORIGINAL initial-sync.ts = 238 with mine (baseline re-measured by git-checkout round-trip; the audit's "39" was an earlier repo state) → 0 new; 0 errors mention initial-sync.ts. tsc packages/core: 32 pre-existing before/after (my change is a comment block). Registry JSON round-trips through require from local-api's relative path.
Stage Summary:
- WAL POST mutations now carry stable content-derived idempotency keys in both logging paths — double-taps/retries of the same logical op actually dedupe via the UNIQUE index and X-Idempotency-Key header; PATCH/DELETE + existing rows untouched.
- Initial sync is complete: 19 stages; the 6 previously-never-imported models (SmsSettings, PaymentSettings, GlobalAnnouncement, PlanFeature, Favorite, FAQ) now flow cloud→desktop with secrets-conscious projections (SmsSettings apiKey excluded) and every stage either agency-scoped or a documented global read matching existing cloud exposure (payment-settings is already public; SmsSettings mirrors the incremental pull's redacted projection).
- LWW is now skew-aware: device-vs-cloud offset estimated from every pull, smoothed/clamped/persisted, applied to the last-write-wins winner decision, and surfaced via /api/sync/status (clockOffsetMs, serverTimestamp); D7 localModifiedSinceSync gate intentionally left skew-immune (frame ambiguity — documented).
- SYNC_TABLES is no longer a hand-copied list: packages/core/sync-registry.json (generated artifact, regenerator script committed) is required by sync-service.js with a frozen fallback; zero set drift found vs sync-registry.ts; AGENCY_SCOPED_TABLES kept local for push-scoping safety (documented); syncModelCount now truthful (19).
- Conflicts routes E2E-verified live (401/200/404/400 paths) with two robustness bugs found and fixed (fresh-DB "no such table" 500; not-found 500→404).
- Next actions (remaining §5): item 1 devices feature decision, item 2 dead-code purge, item 7 Electron local-first P0s (loading-screen dead-end, DB-wipe backup, setCloudSyncAuth call shape, packaging — note electron-builder.yml still doesn't ship local-api/** or packages/core, which the new JSON require's fallback covers but packaging should fix).

---
Task ID: 4-e + 5-a..5-d (closeout)
Agent: main (Z.ai Code)
Task: Apply comprehensive update for all remaining discovered issues (fix wave 4-a/4-b/4-c + probe endpoint), then relaunch the same 4-dimension audit in verification mode (5-a parity, 5-b cloud↔local API, 5-c sync engine, 5-d local-first)

Work Log:
- Launched 3 parallel fix agents: 4-a (Electron local-first P0s), 4-b (sync engine P2s), 4-c (web parity + dead-code purge + cloud duplicates)
- 4-a: loading-screen retry/force-launch + diagnostics:rerun IPC; error-page Retry via retryOffline→loadApp; DB backup before --force-reset (keep 3); setCloudSyncAuth({token,user}); offline-expired Arabic modal + i18n; sync-indicator amber <12h; deleted deprecated local-api-routes/local-db/cloud-sync + yml cleanup + prisma extraResources; /api/health identity marker + 3080 identity gates; local-db:* allowlist; Ctrl+Q wired
- 4-b: stable content-derived idempotency keys (recursive sort + volatile strip + sha256) in both WAL paths; 6 new initial-sync stages cloud+desktop (19 total, agency-scoped, apiKey redacted); server-clock calibration (EMA, ±7d clamp, skew-aware LWW, status fields); sync-registry.json SSOT consumed by sync-service (19/19 match); E2E live: conflicts 401/200/404-not-500, _ensureConflictsTable fresh-DB fix
- 4-c: AgencyDevices wired (ViewRouter case + lazy import + sidebar nav + 'devices' i18n); 20 dead files deleted (12 web + 8 desktop, all zero-importer proven); cloud agency.ts duplicate /hardware + /enterprise-request + /subscription/cancel registrations deleted (234 lines, kept richer set); 9 device endpoints E2E-verified 200
- main: added POST /api/agency-devices/discovery/probe (zod, LAN-only SSRF guard, TCP connect w/ timeout) replacing dead /api/probe?XTransformPort=3010 printer-test call; UI switched to it; live E2E: reachable:true / ECONNREFUSED reachable:false / public-IP 400 / unauth 401
- Re-audit (5-a..5-d, report-only + live E2E): 5-a PASS (parity byte-level, 0 regressions), 5-b 12/12 PASS (0 regressions), 5-c PASS incl. live local-API E2E (1 PARTIAL: suppressAutoSyncChange unused), 5-d PASS (0 regressions; 2 packaging holes pre-existing)
- Closeout fixes for re-audit findings: conflict-resolution-dialog rtl-const bug (both platforms, was always-right-arrow); conflictSingular/conflictPlural i18n keys ×6 dicts replacing hardcoded 'conflict/conflicts'; enterprisePlanDesc split into enterpriseRequestDesc (request dialog) vs enterprisePlanDesc (current-plan display) — desktop had kept the WRONG string after its dedupe; web route-map.ts +7 missing ViewName keys incl agency-devices; turn-alert-sleep stale .tsx comment; suppressAutoSyncChange wired around withIdempotency/atomicMutation in processPushMutation (kills 2-3x redundant SyncChange rows); electron-builder.yml adds loading-screen.js + frontend/dist/**/* (replaces stale out/**/*) + asarUnpack; loading-screen.js version require made layout-defensive; _syncModelCount now sourced from registry (was lying 12); preload onLocalDbChanged orphan listener removed (no sender/consumer existed)

Stage Summary:
- Fix wave verified by independent re-audit: ALL 4 dimensions PASS with 0 regressions
- Web tsc 89→84 errors (enterprisePlanDesc dupes + route-map + isRTL cleared); apps/api tsc 238 = baseline (0 new); lint 39 = prebuilt-bundle baseline; node --check all JS ✅; YAML parses ✅
- Browser E2E (agent-browser): landing ar-RTL → owner1 login → onboarding wizard skip works (store onboarded:true) → dashboard live (يُخدم الآن 7, 3 في الانتظار, متصل بالخادم, CSV, fullscreen) → الأجهزة nav → devices view live (الأجهزة والاتصال, 1 متصل/0 غير متصل, cast/TV-link/kiosk/network-scan buttons) → back to dashboard ✅; 0 page errors, 0 console errors
- Packaging (code-ready, runtime-unverified in sandbox): loading-screen + frontend/dist now packaged; prisma schema/CLI/engines shipped; Electron smoke run still recommended before release
- Remaining known deferreds (documented, non-blocking): update:*/lan:discovery dormant channels (typed placeholders), AgencyHistoryContent split web port, desktop device-feature port (web-only by design), packaged-build electron-builder smoke test

---
Task ID: 6
Agent: main (Z.ai Code)
Task: Fix "when running the api and webapp, cpu and ram usage hits 100% and the pc crashes" — diagnose and fix combined-stack resource exhaustion

Work Log:
- Live-diagnosed the sandbox stack: next-server (v16.2.11) at 1.47GB RSS (35% of RAM, UNCAPPED heap); TWO `bun --hot src/index.ts` API processes — orphan (PPID 1) whose listen() hit EADDRINUSE but was swallowed by the `uncaughtException` keep-alive handler → zombie; `dev:api` spawned Prisma CLI db:push on EVERY boot; `keep-api-alive.sh` = blind `while true … sleep 3` restart loop; `daemon-start.sh`/`start.sh` started services unconditionally (non-idempotent); `bun --hot` on a stateful server silently never applies index.ts changes (re-listen EADDRINUSE swallowed)
- New scripts/dev-api.cjs: single-instance guard (probes :3003/health, refuses to start a second API), sha256 schema-stamp conditional db:push (packages/db/.schema-stamp, FORCE_DB_PUSH=1 override), Node-set env defaults (kills the POSIX `export` chain that broke Windows), spawns `bun --watch` (clean restarts, state never duplicates)
- New scripts/dev-web.cjs: NODE_OPTIONS --max-old-space-size=3072 (BLASTI_WEB_MAX_HEAP_MB override, preserves existing opts) — V8 GCs inside the dev server instead of starving the OS
- apps/api/src/index.ts: httpServer.on('error') → EADDRINUSE prints actionable message + exit(1) (no more zombies); uncaughtException burst guard (10 in 30s → exit, restartable only by health-gated watchdog)
- Rewrote keep-api-alive.sh: health-gated, backoff 3→30s, hard cap 5 consecutive restarts → gives up loudly (cannot restart-spam anymore); daemon-start.sh + start.sh: every service health-gated/idempotent, web via heap-capped launcher, fixed broken `/home/z/my/z/...` cd fallback + missing-discovery-dir handling
- Root + apps/web package.json: dev:api → node scripts/dev-api.cjs; dev:web/dev:web:lan (+web dev/dev:lan) → node scripts/dev-web.cjs; added .schema-stamp to .gitignore; chmod +x shell scripts
- Sandbox ops: killed old tree + zombie (ports freed), relaunched via new daemon-start.sh, verified single instances; guards re-tested live: `bun run dev:api` against healthy API → starts nothing exit 0; daemon second run → "skipping (no duplicate instance)"; NODE_OPTIONS confirmed in next-server /proc environ
- Verification: node --check both .cjs, bash -n all 3 shell scripts, `bun build` compiles index.ts; web-startup-test external mode 7/7; browser E2E landing (ar-RTL) → owner1 agency login → dashboard live (7 being served, onboarding wizard, CSV) with 0 page errors, all API 200; steady state: next-server 1.31GB heap-capped, API 156MB @ 2.2% CPU, 1.5GB RAM still free; WEB_DEV_CRASH_AUDIT.md addendum (§A root causes table, §B fixes, §C verification, §D safe-run guide) appended

Stage Summary:
- Combined API+web dev stack is now resource-bounded by construction: single-instance guards (launcher pre-flight + EADDRINUSE exit + idempotent daemon/start/watchdog), heap-capped web dev server, schema-stamp cheap boots, bounded watchdog — no path left that can multiply processes, grow RAM unbounded, or restart-spam CPU
- Windows bonus: dev:api no longer depends on POSIX `export` (was silently broken there); all launchers are plain `node scripts/*.cjs`
- Full report: WEB_DEV_CRASH_AUDIT.md addendum (end of file)
